import React from "react";
import MapPage from "./pages/MapPage";

// If you have global providers like React Query Client, Zustand Provider, they might go here
// or in main.tsx depending on your setup preference.

const App: React.FC = () => {
  return <MapPage />;
};

export default App;
